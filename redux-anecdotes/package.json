{
  "name": "redux-anecdotes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd redux-anecdotes-frontend && npm install --legacy-peer-deps && npm run build && cd .. && cp -r redux-anecdotes-frontend/dist .",
    "build_windows": "cd redux-anecdotes-frontend && npm  && cd .. && Copy-Item -Path `redux-anecdotes-frontend/dist` -Destination `` -Recurse"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
